<Window x:Class="GoldenTasksDesktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GoldenTasksDesktop.ViewModels" 
        xmlns:local="clr-namespace:GoldenTasksDesktop.Views"
        mc:Ignorable="d"
        Title="Inicio de Sesión" Height="450" Width="800">

    <StackPanel>
        <Label Content="Usuario o Email"></Label>
        <TextBox Text="{Binding UserNameOrEmail, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Contraseña"></Label>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <!-- Luego cambiar a PasswordBox-->
        <Button Content="Iniciar Sesión" Command="{Binding IniciarSesionCommand}"></Button>
        <Label Content="{Binding Mensaje, UpdateSourceTrigger=PropertyChanged}"></Label>
    </StackPanel>

</Window>

<Window x:Class="GoldenTasksDesktop.Views.CrearUsuarioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoldenTasksDesktop.Views"
        mc:Ignorable="d"
        Title="Crear Usuario" Height="450" Width="800">
    <StackPanel>
        <Label Content="Nombre"></Label>
        <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Email"></Label>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Usuario"></Label>
        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Contraseña"></Label>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Confirmar Contraseña"></Label>
        <TextBox Text="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}"/>
        <!-- Luego hacerlo con PasswordBox.-->
        <Button Content="Crear Usuario" Command="{Binding CrearUsuarioCommand}"></Button>
        <TextBlock Text="{Binding Mensaje}"></TextBlock>
        <Button Content="Regresar"></Button>
    </StackPanel>
</Window>
